defmodule Sondas.MovingSondaTest do
  use ExUnit.Case

  alias Sondas.Services.MovingSonda

  test "Checking for correct probe move ments" do

    # sonda = Sondas.Structs.Sonda.new([1, 2], "n")
    # grid = Sondas.Structs.Planalto.new([5, 5])

    # assert MovingSonda.action_sonda(sonda, grid) == [1, 2, "e"]
    # # assert MovingSonda.action_sonda("LMML") == [1, 2, "e"]


  end

end
